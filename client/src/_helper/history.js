import { createBrowserHistory } from "history"; //https://www.npmjs.com/package/history

//The history library lets you easily manage browser session history anywhere JavaScript runs.
export const history = createBrowserHistory();
